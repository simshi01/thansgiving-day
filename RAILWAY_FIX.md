# 🔧 Исправление проблем с Railway

## Проблема: Postgres спит / БД не подключена

### Шаг 1: Проверка переменных окружения

1. Откройте ваш проект в Railway
2. Перейдите во вкладку **"Variables"**
3. Убедитесь, что есть переменная `DATABASE_URL`
   - Если её нет → см. Шаг 2
   - Если есть → проверьте формат: `postgresql://...`

### Шаг 2: Подключение PostgreSQL

1. В проекте Railway нажмите **"+ New"**
2. Выберите **"Database"** → **"Add PostgreSQL"**
3. Дождитесь создания БД (1-2 минуты)
4. Railway автоматически добавит `DATABASE_URL` в переменные окружения

### Шаг 3: Настройка порта

1. В Railway откройте вкладку **"Settings"** → **"Networking"**
2. Убедитесь, что порт настроен правильно:
   - Railway должен автоматически установить переменную `PORT`
   - Если порт указан как 8080 в настройках домена, Railway автоматически установит `PORT=8080`
3. **Важно**: Не нужно вручную добавлять переменную PORT - Railway делает это автоматически!

### Шаг 4: Настройка Socket.io URL

1. В Railway откройте вкладку **"Variables"**
2. Найдите или добавьте переменную:
   - **Key**: `NEXT_PUBLIC_SOCKET_URL`
   - **Value**: Ваш Railway URL (например: `https://thansgiving-day-production.up.railway.app`)
3. **Важно**: URL должен быть без слеша в конце!

**Как узнать ваш URL:**
- Откройте вкладку **"Settings"** → **"Domains"**
- Или посмотрите в **"Deployments"** → последний деплой

### Шаг 5: Пробуждение Postgres (если спит)

1. Откройте PostgreSQL сервис в Railway
2. Если он спит, просто сделайте запрос к БД:
   - Откройте ваш сайт
   - Отправьте тестовое сообщение
   - Postgres должен проснуться автоматически

### Шаг 6: Проверка работы

1. Откройте ваш сайт по Railway URL
2. Отправьте тестовое сообщение
3. Откройте `/messages` в другой вкладке
4. Сообщение должно появиться в реальном времени

### Шаг 7: Проверка логов

Если что-то не работает:

1. В Railway откройте вкладку **"Deployments"**
2. Нажмите на последний деплой
3. Нажмите **"View logs"**
4. Проверьте наличие ошибок:
   - `DATABASE_URL is not set` → добавьте PostgreSQL (Шаг 2)
   - `Error creating database table` → проверьте `DATABASE_URL`
   - `Socket.io connection failed` → проверьте `NEXT_PUBLIC_SOCKET_URL`

## ✅ Чеклист

- [ ] PostgreSQL добавлен в проект
- [ ] Переменная `DATABASE_URL` существует
- [ ] Переменная `NEXT_PUBLIC_SOCKET_URL` настроена на ваш Railway URL
- [ ] Деплой успешно завершен (зеленая галочка)
- [ ] Postgres не спит (или проснулся после первого запроса)
- [ ] Сайт открывается по Railway URL
- [ ] Сообщения сохраняются и отображаются

## 🐛 Частые проблемы

### "Application failed to respond"
**Решение:**
1. Проверьте логи в Railway → "Deployments" → последний деплой → "View logs"
2. Убедитесь, что порт правильный:
   - В Railway Settings → Networking порт должен быть 8080 (или другой, который Railway установил)
   - Railway автоматически установит переменную `PORT`
3. Проверьте что сервер запустился - в логах должно быть: `✅ Server listening on http://0.0.0.0:8080`
4. Если порт не совпадает - измените порт в Railway Settings → Networking на тот, который указан в логах

### "DATABASE_URL is not set"
**Решение:** Добавьте PostgreSQL через "+ New" → "Database" → "Add PostgreSQL"

### Postgres спит
**Решение:** Это нормально! Postgres просыпается автоматически при первом запросе. Просто отправьте тестовое сообщение.

### Socket.io не работает
**Решение:** 
1. Проверьте `NEXT_PUBLIC_SOCKET_URL` - должен быть ваш Railway URL
2. Убедитесь, что URL без слеша в конце
3. Проверьте логи в Railway

### Сообщения не сохраняются
**Решение:**
1. Проверьте логи в Railway
2. Убедитесь, что `DATABASE_URL` правильный
3. Проверьте, что таблица создалась (должно быть в логах: "✅ Database table initialized successfully")

