# 🚀 Быстрый старт с Railway

## Что уже сделано

✅ Установлены все зависимости  
✅ Создана структура БД (PostgreSQL)  
✅ Реализованы API endpoints  
✅ Настроен Socket.io для реального времени  
✅ Добавлена модерация текста  
✅ Интегрирован фронтенд с бэкендом  

## 📝 Что нужно сделать ВАМ

### 1. Регистрация на Railway (5 минут)

1. Откройте [railway.app](https://railway.app)
2. Нажмите **"Start a New Project"**
3. Войдите через **GitHub**
4. Подтвердите email

### 2. Подключение репозитория (2 минуты)

1. Нажмите **"New Project"** → **"Deploy from GitHub repo"**
2. Если репозиторий не подключен:
   - **"Configure GitHub App"**
   - Выберите репозиторий `thanksgiving-day`
   - **"Install"**
3. Выберите репозиторий `thanksgiving-day`
4. Railway начнет автоматический деплой

### 3. Добавление PostgreSQL (1 минута)

1. В проекте нажмите **"+ New"**
2. Выберите **"Database"** → **"Add PostgreSQL"**
3. Готово! Railway автоматически добавит `DATABASE_URL`

### 4. Настройка переменных окружения (1 минута)

1. Откройте вкладку **"Variables"**
2. Найдите `DATABASE_URL` (должна быть автоматически)
3. Добавьте новую переменную:
   - **Key**: `NEXT_PUBLIC_SOCKET_URL`
   - **Value**: Ваш URL (Railway покажет его после деплоя)
   - Формат: `https://your-project-name.up.railway.app`

**Как узнать URL:**
- После деплоя Railway создаст URL
- Он будет в **"Settings"** → **"Domains"**
- Или в **"Deployments"** → последний деплой

### 5. Ожидание деплоя (2-5 минут)

1. Следите за процессом в **"Deployments"**
2. Дождитесь зеленой галочки ✅
3. Откройте ваш сайт по URL

### 6. Проверка работы

1. Откройте главную страницу
2. Отправьте тестовое сообщение
3. Откройте `/messages` в другой вкладке
4. Сообщение должно появиться в реальном времени!

---

## 🔧 Локальная разработка

### 1. Создайте `.env.local`:

```env
DATABASE_URL=postgresql://user:password@host:port/dbname
NEXT_PUBLIC_SOCKET_URL=http://localhost:3000
```

**Как получить DATABASE_URL:**
- В Railway откройте PostgreSQL сервис
- Перейдите в **"Connect"** или **"Variables"**
- Скопируйте `DATABASE_URL`

### 2. Запустите проект:

```bash
npm run dev
```

Таблица создастся автоматически при первом запросе.

---

## 📁 Структура проекта

```
thanksgiving-day/
├── app/
│   ├── api/
│   │   ├── messages/route.ts    # API для сообщений
│   │   └── socket/route.ts      # Socket.io endpoint
│   ├── messages/page.tsx        # Страница с сообщениями
│   └── page.tsx                 # Главная страница
├── components/
│   ├── MessageBubble.tsx
│   └── MessageContainer.tsx     # Обновлен для Socket.io
├── lib/
│   ├── db.ts                    # PostgreSQL функции
│   ├── moderation.ts            # Модерация текста
│   └── socket-server.ts         # Socket.io сервер
├── server.ts                    # Custom server для Socket.io
├── RAILWAY_SETUP.md            # Подробная инструкция
└── package.json
```

---

## ⚠️ Важные моменты

1. **Переменная `NEXT_PUBLIC_SOCKET_URL`** должна указывать на ваш Railway URL
2. **Таблица создается автоматически** при первом запросе к API
3. **Socket.io работает** через custom server (`server.ts`)
4. **Модерация** проверяет текст на мат и плохие слова
5. **Лимит длины** сообщения: 300 символов

---

## 🐛 Решение проблем

### "DATABASE_URL is not set"
- Убедитесь, что PostgreSQL добавлен в проект
- Проверьте переменные окружения в Railway

### Socket.io не работает
- Проверьте `NEXT_PUBLIC_SOCKET_URL`
- Убедитесь, что URL правильный
- Проверьте логи в Railway

### Сообщения не сохраняются
- Проверьте логи в Railway
- Убедитесь, что таблица создана
- Проверьте подключение к БД

---

## 💰 Стоимость

**Бесплатный план Railway:**
- $5 кредитов в месяц
- Достаточно для 100+ пользователей
- PostgreSQL включен
- Обычно хватает на весь месяц бесплатно

---

## ✅ Чеклист

- [ ] Регистрация на Railway
- [ ] Подключение GitHub репозитория
- [ ] Добавление PostgreSQL
- [ ] Настройка `NEXT_PUBLIC_SOCKET_URL`
- [ ] Успешный деплой
- [ ] Проверка работы сайта
- [ ] Тест отправки сообщения
- [ ] Проверка синхронизации

---

**Готово! 🎉** После выполнения всех шагов ваш сервис будет работать!

