# Инструкция по настройке Railway

## 📋 Что нужно сделать

### Шаг 1: Регистрация на Railway

1. Перейдите на [railway.app](https://railway.app)
2. Нажмите **"Start a New Project"** или **"Login"**
3. Войдите через **GitHub** (рекомендуется) или через email
4. Подтвердите email, если потребуется

---

### Шаг 2: Подключение репозитория

1. После входа нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Если репозиторий еще не подключен:
   - Нажмите **"Configure GitHub App"**
   - Выберите репозиторий `thanksgiving-day`
   - Нажмите **"Install"**
4. Выберите ваш репозиторий `thanksgiving-day`
5. Railway автоматически определит Next.js и начнет деплой

---

### Шаг 3: Добавление PostgreSQL базы данных

1. В вашем проекте на Railway нажмите **"+ New"**
2. Выберите **"Database"** → **"Add PostgreSQL"**
3. Railway автоматически создаст PostgreSQL базу данных
4. **Важно**: Railway автоматически добавит переменную окружения `DATABASE_URL` в ваш проект

---

### Шаг 4: Настройка переменных окружения

1. В вашем проекте на Railway откройте вкладку **"Variables"**
2. Убедитесь, что есть переменная `DATABASE_URL` (должна быть добавлена автоматически)
3. Добавьте новую переменную:
   - **Key**: `NEXT_PUBLIC_SOCKET_URL`
   - **Value**: `https://your-project-name.up.railway.app` (замените на ваш URL)
   - Нажмите **"Add"**

**Как узнать ваш URL:**
- После деплоя Railway создаст публичный URL
- Он будет показан в разделе **"Settings"** → **"Domains"**
- Или в разделе **"Deployments"** → выберите последний деплой → там будет URL

---

### Шаг 5: Ожидание деплоя

1. Railway автоматически запустит деплой после подключения репозитория
2. Вы можете следить за процессом в разделе **"Deployments"**
3. Деплой обычно занимает 2-5 минут
4. После успешного деплоя вы увидите зеленую галочку ✅

---

### Шаг 6: Проверка работы

1. Откройте ваш сайт по URL, который предоставил Railway
2. Попробуйте отправить сообщение на главной странице
3. Откройте страницу `/messages` в другой вкладке
4. Сообщение должно появиться на странице `/messages` в реальном времени

---

## 🔧 Настройка для локальной разработки

### 1. Создайте файл `.env.local` в корне проекта:

```env
DATABASE_URL=postgresql://user:password@host:port/dbname
NEXT_PUBLIC_SOCKET_URL=http://localhost:3000
```

**Как получить DATABASE_URL для локальной разработки:**

**Вариант 1: Использовать Railway PostgreSQL**
1. В Railway откройте ваш PostgreSQL сервис
2. Перейдите в **"Connect"** или **"Variables"**
3. Скопируйте `DATABASE_URL`
4. Вставьте в `.env.local`

**Вариант 2: Установить PostgreSQL локально**
1. Установите PostgreSQL на ваш компьютер
2. Создайте базу данных: `createdb thanksgiving_day`
3. Используйте: `postgresql://localhost:5432/thanksgiving_day`

### 2. Запустите миграцию БД:

```bash
npm run dev
```

При первом запросе к API таблица создастся автоматически.

Или создайте файл `scripts/init-db.ts` и запустите его один раз:

```typescript
import { createMessagesTable } from '../lib/db'

createMessagesTable()
  .then(() => {
    console.log('Database initialized successfully')
    process.exit(0)
  })
  .catch((error) => {
    console.error('Error initializing database:', error)
    process.exit(1)
  })
```

---

## 📝 Структура проекта

После настройки у вас будет:

```
thanksgiving-day/
├── app/
│   ├── api/
│   │   ├── messages/
│   │   │   └── route.ts          # API для сообщений
│   │   └── socket/
│   │       └── route.ts          # Socket.io endpoint
│   ├── messages/
│   │   └── page.tsx              # Страница с сообщениями
│   └── page.tsx                   # Главная страница
├── components/
│   ├── MessageBubble.tsx
│   └── MessageContainer.tsx
├── lib/
│   ├── db.ts                      # Работа с PostgreSQL
│   ├── moderation.ts              # Модерация текста
│   └── socket-server.ts           # Socket.io сервер
├── .env.local                     # Локальные переменные (не коммитить!)
└── package.json
```

---

## 🚀 Деплой на Railway

Railway автоматически деплоит при каждом push в GitHub:

1. Сделайте изменения в коде
2. Закоммитьте и запушьте в GitHub:
   ```bash
   git add .
   git commit -m "Your changes"
   git push
   ```
3. Railway автоматически начнет новый деплой
4. Через 2-5 минут изменения будут на сайте

---

## 🔍 Проверка логов

Если что-то не работает:

1. В Railway откройте ваш проект
2. Перейдите в раздел **"Deployments"**
3. Выберите последний деплой
4. Откройте вкладку **"Logs"**
5. Там будут видны все ошибки и логи

---

## ⚠️ Возможные проблемы

### Проблема: "DATABASE_URL is not set"
**Решение**: 
- Убедитесь, что PostgreSQL сервис добавлен в проект
- Проверьте переменные окружения в Railway

### Проблема: Socket.io не работает
**Решение**:
- Проверьте переменную `NEXT_PUBLIC_SOCKET_URL`
- Убедитесь, что она указывает на правильный URL
- Проверьте логи в Railway

### Проблема: Сообщения не сохраняются
**Решение**:
- Проверьте подключение к БД в логах
- Убедитесь, что таблица создана (проверьте логи при первом запросе)

---

## 💰 Стоимость

**Бесплатный план Railway:**
- $5 кредитов в месяц
- Достаточно для 100+ пользователей
- PostgreSQL включен бесплатно
- Обычно хватает на весь месяц бесплатно

**Если превысите лимит:**
- Railway уведомит вас
- Можно перейти на платный план ($5-20/месяц)
- Или оптимизировать использование

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в Railway
2. Проверьте переменные окружения
3. Убедитесь, что PostgreSQL сервис запущен
4. Проверьте, что код задеплоился без ошибок

---

## ✅ Чеклист перед запуском

- [ ] Регистрация на Railway
- [ ] Подключение GitHub репозитория
- [ ] Добавление PostgreSQL базы данных
- [ ] Настройка переменных окружения (`DATABASE_URL` и `NEXT_PUBLIC_SOCKET_URL`)
- [ ] Успешный деплой проекта
- [ ] Проверка работы сайта
- [ ] Тест отправки сообщения
- [ ] Проверка синхронизации в реальном времени

---

**Готово! 🎉** После выполнения всех шагов ваш сервис будет работать в интернете!

