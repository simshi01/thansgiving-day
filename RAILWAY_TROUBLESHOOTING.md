# 🔍 Диагностика проблемы "Application failed to respond"

## Быстрая проверка

### 1. Проверьте логи деплоя

1. В Railway откройте **"Deployments"**
2. Нажмите на последний деплой
3. Нажмите **"View logs"**
4. Ищите ошибки или сообщения:

**Что должно быть:**
```
✅ Next.js prepared
✅ Server listening on http://0.0.0.0:8080
✅ Application ready!
```

**Если видите ошибки:**
- `Error: Cannot find module` → проблема с зависимостями
- `Error: PORT is not defined` → Railway не установил PORT
- `Error: Database connection failed` → проблема с DATABASE_URL

### 2. Проверьте порт в Railway

1. Откройте **"Settings"** → **"Networking"**
2. Проверьте какой порт указан (должен быть 8080 или другой)
3. Railway автоматически установит переменную `PORT` с этим значением
4. **Важно**: Не нужно вручную добавлять переменную PORT!

### 3. Проверьте переменные окружения

В Railway → **"Variables"** должны быть:
- `PORT` - автоматически устанавливается Railway (не добавляйте вручную!)
- `DATABASE_URL` - автоматически после добавления PostgreSQL
- `NEXT_PUBLIC_SOCKET_URL` - ваш Railway URL (например: `https://thansgiving-day-production.up.railway.app`)

### 4. Проверьте что build прошел успешно

В логах деплоя должна быть секция **"Build"**:
- Должно быть: `Build completed successfully`
- Не должно быть ошибок компиляции TypeScript

### 5. Проверьте healthcheck

После деплоя попробуйте открыть:
```
https://your-app.up.railway.app/health
```

Должен вернуться JSON:
```json
{"status":"ok","timestamp":"..."}
```

## Решения проблем

### Проблема: Порт не совпадает

**Симптомы:** В логах порт отличается от того что в Railway Settings

**Решение:**
1. В Railway Settings → Networking измените порт на тот, который указан в логах
2. Или наоборот - проверьте что Railway установил правильный PORT

### Проблема: Build не прошел

**Симптомы:** Ошибки компиляции в логах

**Решение:**
1. Проверьте что все зависимости установлены: `npm install`
2. Проверьте что TypeScript компилируется: `npm run build`
3. Убедитесь что все файлы закоммичены

### Проблема: Сервер не запускается

**Симптомы:** В логах нет сообщения "Server listening"

**Решение:**
1. Проверьте что `npm start` запускает `tsx server.ts`
2. Убедитесь что `tsx` установлен в dependencies
3. Проверьте что `server.ts` существует и без синтаксических ошибок

### Проблема: Timeout при старте

**Симптомы:** Railway не может подключиться к приложению

**Решение:**
1. Убедитесь что сервер слушает на `0.0.0.0` (не `localhost` или `127.0.0.1`)
2. Проверьте что порт правильный
3. Убедитесь что healthcheck endpoint работает: `/health`

## Что делать сейчас

1. **Откройте логи** в Railway и скопируйте последние 50-100 строк
2. **Проверьте порт** в Settings → Networking
3. **Проверьте переменные** в Variables
4. **Попробуйте пересобрать** - сделайте новый коммит или нажмите "Redeploy"

## Если ничего не помогло

1. Убедитесь что локально все работает: `npm run dev`
2. Проверьте что build проходит: `npm run build`
3. Проверьте что start работает: `npm start` (должен запуститься на порту 3000)

